#include<iostream>
#include<string>
#include<vector>
#include<fstream>
using namespace std;
int main()
{
	string s,t;
	while(cin>>s>>t){
		int i=0;
		for(int j=0;i<s.length()&&j<t.length();j++)
			if(s[i]==t[j])
				i++;
		if(i==s.length())puts("Yes");
		else puts("No");
	}
	return 0;
}
